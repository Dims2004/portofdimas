server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Semua rute diarahkan ke index.html supaya React Router
    # (/karya, /karya/:slug, /cerita, dst) tidak 404 saat di-refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
